name: Insight Crossroad Auto Publisher

on:
  workflow_dispatch:

jobs:
  publish-post:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Run profit blog system
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        UNSPLASH_API_KEY: ${{ secrets.UNSPLASH_API_KEY }}
        BLOGGER_API_KEY: ${{ secrets.BLOGGER_API_KEY }}
        BLOGGER_BLOG_ID: ${{ secrets.BLOGGER_BLOG_ID }}
        OAUTH_CLIENT_ID: ${{ secrets.OAUTH_CLIENT_ID }}
        OAUTH_CLIENT_SECRET: ${{ secrets.OAUTH_CLIENT_SECRET }}
        OAUTH_REFRESH_TOKEN: ${{ secrets.OAUTH_REFRESH_TOKEN }}
      run: |
        python profit_blog.py
```

8. **"Commit changes"** 클릭

9. **"Commit changes"** 다시 클릭

---

## STEP 4: 테스트

### 실행하기

1. **Repository 메인 페이지로**

2. **"Actions"** 탭 클릭

3. 왼쪽에서 **"Insight Crossroad Auto Publisher"** 클릭

4. **"Run workflow"** 버튼 클릭 (오른쪽)

5. **"Run workflow"** 다시 클릭 (초록색 버튼)

6. **새로고침** (F5)

7. **노란 점** 보이면 실행 중

8. **클릭해서 로그 보기**

### 성공 확인

**로그에서 이런 메시지 보이면 성공:**
```
✅ Claude 주제 생성 완료
📝 주제: Why 70% of...
✅ Claude 본문 생성 완료 (15234 문자)
🎉 글 발행 완료!
